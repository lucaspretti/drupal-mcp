{
  "name": "drupal-mcp",
  "version": "0.1.0",
  "description": "MCP server for Drupal sites via JSON:API. Read and write nodes, taxonomy terms, users — anything the JSON:API exposes.",
  "main": "drupal-mcp.js",
  "type": "module",
  "bin": {
    "drupal-mcp": "drupal-mcp.js"
  },
  "scripts": {
    "start": "node drupal-mcp.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lucaspretti/drupal-mcp.git"
  },
  "keywords": ["mcp", "drupal", "jsonapi", "claude"]
}
